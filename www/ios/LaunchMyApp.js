"use strict";


function handleOpenURL(url) {
  setTimeout(function() {
    window._openURL = url;
  }, 0);
}
function handleOpenUrl(url) {
  setTimeout(function() {
    window._openUrl = url;
  }, 0);
}
