"use strict";


function handleOpenURL(url) {
  setTimeout(function() {
    window._openURL = url;
  }, 250);
}
function handleOpenUrl(url) {
  setTimeout(function() {
    window._openUrl = url;
  }, 250);
}
